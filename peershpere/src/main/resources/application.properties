
# ?? Application ??????????????????????????????????????????
spring.application.name=peersphere

# ?? Database ??????????????????????????????????????????????
spring.datasource.url=jdbc:mysql://localhost:3306/peersphere_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Naveen06
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ?? JPA / Hibernate ???????????????????????????????????????
# 'update' auto-creates/alters tables based on your @Entity classes
# In production you'd use 'validate' or Flyway migrations
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ?? Server ????????????????????????????????????????????????
server.port=8081

# ?? JWT (we'll use these in the next module) ??????????????
app.jwt.secret=PeerSphereSecretKey2024SuperLongSecretKeyForHS256AlgorithmMinimum256Bits
app.jwt.expiration=86400000

# ?? Google Calendar Integration ???????????????????????????
google.calendar.credentials-file=classpath:google-credentials.json
google.calendar.application-name=PeerSphere
# This is the service account's own calendar ? or use "primary"
google.calendar.calendar-id=primary